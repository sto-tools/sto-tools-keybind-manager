/**
 * Application constants
 */

// Current application version
export const APP_VERSION = '2025.06.22-beta-1'

// Display version with 'v' prefix for UI
export const DISPLAY_VERSION = `v${APP_VERSION}`
